'use client'

import Link from 'next/link'
import { useState } from 'react'
import { ArrowRight, BookOpen, Check, ChevronDown, CircleHelp, Clock3, FileText, GraduationCap, Lightbulb, LoaderCircle, Menu, Moon, MoreHorizontal, Play, Search, Sparkles, Sun, Target, X, Zap } from 'lucide-react'

type Session = { title: string; explanation: string; keyPoints: string[]; examples: { title: string; explanation: string }[]; practice: { prompt: string; answer: string; hint: string }[]; quiz: { question: string; options: string[]; correctIndex: number; explanation: string }[]; summary: string }
const subjects = [{ name: 'Mathematics', detail: 'Algebra, calculus & more', icon: '∑', color: 'violet' }, { name: 'Physics', detail: 'Forces, energy & motion', icon: '◉', color: 'blue' }, { name: 'Biology', detail: 'Life, cells & systems', icon: '⌁', color: 'green' }, { name: 'History', detail: 'People, places & time', icon: '✦', color: 'amber' }]
const activities = [{ title: 'Quadratic Equations', subject: 'Mathematics', score: '92%', time: 'Today', color: 'violet' }, { title: 'Newton’s Laws of Motion', subject: 'Physics', score: '84%', time: 'Yesterday', color: 'blue' }, { title: 'Cellular Respiration', subject: 'Biology', score: '76%', time: 'May 18', color: 'green' }]

function Brand() { return <div className="brand"><div className="brand-mark"><Sparkles /></div><span>Luma</span></div> }
function Sidebar({ active, setActive }: { active: string; setActive: (value: string) => void }) { const nav = [{ label: 'Workspace', icon: Target }, { label: 'My progress', icon: GraduationCap }, { label: 'Saved topics', icon: BookOpen }]; return <aside className="sidebar"><Brand /><div className="sidebar-label">Learn</div><nav className="side-nav" aria-label="Main navigation">{nav.map(({ label, icon: Icon }) => <button key={label} className={`nav-item ${active === label ? 'active' : ''}`} onClick={() => setActive(label)}><Icon />{label}</button>)}<Link href="/about" className="nav-item"><CircleHelp />About LumaLearn</Link></nav><div className="sidebar-label">Your space</div><nav className="side-nav"><button className="nav-item"><Target />Learning goals</button><button className="nav-item"><Zap />Study streak</button></nav><div className="sidebar-bottom"><div className="upgrade-card"><div className="upgrade-icon"><Zap /></div><p>Make every study session count.</p><button onClick={() => setActive('My progress')}>View progress <ArrowRight /></button></div><div className="user-row"><div className="avatar">AM</div><div><strong>Alex Morgan</strong><span>Student account</span></div><MoreHorizontal className="more" /></div></div></aside> }
function SelectPill({ label, value, onClick }: { label: string; value: string; onClick: () => void }) { return <button className="select-pill" onClick={onClick}><span className="pill-label">{label}</span><strong>{value}</strong><ChevronDown /></button> }
function RichText({ text }: { text: string }) { return <div className="rich-text">{text.split('\n').map((line, index) => <p key={index}>{line || '\u00a0'}</p>)}</div> }
function LearningSession({ session, onReset }: { session: Session; onReset: () => void }) { const [answers, setAnswers] = useState<number[]>([]); const score = answers.reduce((total, answer, index) => total + (answer === session.quiz[index]?.correctIndex ? 1 : 0), 0); return <section className="learning-session"><div className="session-heading"><div><p className="eyebrow">Your guided learning path</p><h2>{session.title}</h2><p>Read through the guide, then check your understanding with the quiz.</p></div><button className="secondary-button" onClick={onReset}>New session</button></div><div className="learning-grid"><article className="lesson-card main-lesson"><div className="lesson-label"><Sparkles /> Explanation</div><RichText text={session.explanation} /><h3>Key ideas</h3><ul>{session.keyPoints.map((point) => <li key={point}><Check />{point}</li>)}</ul></article><article className="lesson-card"><div className="lesson-label"><Lightbulb /> Examples</div>{session.examples.map((example) => <div className="example-block" key={example.title}><strong>{example.title}</strong><RichText text={example.explanation} /></div>)}</article></div><article className="lesson-card practice-card"><div className="lesson-label"><FileText /> Practice</div><div className="practice-list">{session.practice.map((item, index) => <div className="practice-item" key={item.prompt}><strong>{index + 1}. {item.prompt}</strong><details><summary>Show hint and answer</summary><p><b>Hint:</b> {item.hint}</p><p><b>Answer:</b> {item.answer}</p></details></div>)}</div></article><article className="lesson-card quiz-card"><div className="quiz-top"><div><div className="lesson-label"><Target /> Knowledge check</div><h3>Quick quiz</h3></div>{answers.length > 0 && <span className="quiz-score">{answers.length === session.quiz.length ? `${score}/${session.quiz.length} correct` : `${answers.length}/${session.quiz.length} answered`}</span>}</div>{session.quiz.map((question, index) => { const selected = answers[index]; const answered = selected !== undefined; return <div className="question" key={question.question}><p><b>{index + 1}.</b> {question.question}</p><div className="options">{question.options.map((option, optionIndex) => <button key={option} className={`${answered ? optionIndex === question.correctIndex ? 'correct' : selected === optionIndex ? 'incorrect' : '' : selected === optionIndex ? 'selected' : ''}`} onClick={() => setAnswers((current) => { const next = [...current]; next[index] = optionIndex; return next })} disabled={answered}><span>{String.fromCharCode(65 + optionIndex)}</span>{option}</button>)}</div>{answered && <div className={`answer-note ${selected === question.correctIndex ? 'is-correct' : 'is-incorrect'}`}><CircleHelp />{selected === question.correctIndex ? 'Correct. ' : 'Not quite. '}{question.explanation}</div>}</div>})}</article><article className="summary-card"><div className="lesson-label"><GraduationCap /> Session summary</div><RichText text={session.summary} /></article></section> }

export default function Page() { const [active, setActive] = useState('Workspace'); const [mobileOpen, setMobileOpen] = useState(false); const [dark, setDark] = useState(true); const [subject, setSubject] = useState('Mathematics'); const [topic, setTopic] = useState(''); const [level, setLevel] = useState('University'); const [difficulty, setDifficulty] = useState('Balanced'); const [status, setStatus] = useState<'idle' | 'loading' | 'error'>('idle'); const [session, setSession] = useState<Session | null>(null); const [error, setError] = useState('');
 async function generate() { if (!topic.trim()) return; setStatus('loading'); setError(''); setSession(null); try { const response = await fetch('/api/learning', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ subject, topic, level, difficulty }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Generation failed'); setSession(data); setStatus('idle'); setTimeout(() => document.querySelector('.learning-session')?.scrollIntoView({ behavior: 'smooth', block: 'start' }), 50) } catch (caught) { setError(caught instanceof Error ? caught.message : 'Something went wrong. Please try again.'); setStatus('error') } }
 return <div className={dark ? 'app-shell dark-mode' : 'app-shell'}><button className="mobile-menu" aria-label="Open navigation" onClick={() => setMobileOpen(true)}><Menu /></button>{mobileOpen && <div className="mobile-overlay" onClick={() => setMobileOpen(false)}><div className="mobile-drawer" onClick={(event) => event.stopPropagation()}><button className="drawer-close" onClick={() => setMobileOpen(false)}><X /></button><Sidebar active={active} setActive={(value) => { setActive(value); setMobileOpen(false) }} /></div></div>}<Sidebar active={active} setActive={setActive} /><main className="main-content"><header className="topbar"><div className="breadcrumb"><span>Workspace</span><span>/</span><strong>{active}</strong></div><div className="top-actions"><button className="icon-button" aria-label="Search"><Search /></button><button className="icon-button" aria-label="Toggle theme" onClick={() => setDark(!dark)}>{dark ? <Sun /> : <Moon />}</button><div className="top-avatar">AM</div></div></header><div className="content-wrap"><section className="hero"><div><p className="eyebrow">Tuesday, May 21, 2024 <span className="status-dot" /></p><h1>Good morning, Alex<span>.</span></h1><p className="hero-copy">What would you like to understand today?</p></div><div className="streak"><div className="streak-icon"><Zap /></div><div><strong>7 day streak</strong><span>Keep your momentum going</span></div></div></section><section className="setup-card"><div className="setup-head"><div><div className="section-kicker"><Sparkles /> Learn something new</div><h2>Build your learning session</h2><p>Choose a topic and Luma will guide you through a focused lesson from its study library.</p></div><div className="step-count"><span>01</span><div /><span>02</span><div /><span>03</span></div></div><div className="subject-grid">{subjects.map((item) => <button key={item.name} className={`subject-card ${subject === item.name ? 'selected' : ''} ${item.color}`} onClick={() => setSubject(item.name)}><span className="subject-symbol">{item.icon}</span><span><strong>{item.name}</strong><small>{item.detail}</small></span>{subject === item.name && <span className="check"><Check /></span>}</button>)}</div><div className="session-controls"><label className="topic-field"><span>What are you studying?</span><div className="input-wrap"><Lightbulb /><input value={topic} onChange={(event) => { setTopic(event.target.value); setStatus('idle') }} placeholder="e.g. Integration by parts" onKeyDown={(event) => { if (event.key === 'Enter' && !event.nativeEvent.isComposing && event.keyCode !== 229) generate() }} /><span className="optional">Required</span></div></label><div className="select-group"><SelectPill label="Academic level" value={level} onClick={() => setLevel(level === 'University' ? 'High school' : 'University')} /><SelectPill label="Difficulty" value={difficulty} onClick={() => setDifficulty(difficulty === 'Balanced' ? 'Challenge' : 'Balanced')} /></div></div><div className="setup-footer"><span className="ai-note"><CircleHelp /> Structured explanations, examples, practice and quizzes.</span><button className="generate-button" onClick={generate} disabled={!topic.trim() || status === 'loading'}>{status === 'loading' ? <><LoaderCircle className="spin" /> Building your path...</> : <>Start learning<ArrowRight /></>}</button></div>{status === 'error' && <div className="error-state"><CircleHelp /><span><strong>Couldn’t create your session.</strong> {error}</span><button onClick={generate}>Try again</button></div>}</section>{session ? <LearningSession session={session} onReset={() => { setSession(null); setTopic(''); window.scrollTo({ top: 0, behavior: 'smooth' }) }} /> : <section className="dashboard-section"><div className="section-title-row"><div><p className="eyebrow">Your learning space</p><h2>Keep making progress</h2></div></div><div className="stats-grid"><div className="stat-card stat-highlight"><div className="stat-icon"><Target /></div><span>Learning goal</span><strong>14 <small>/ 20 hrs</small></strong><div className="progress-track"><div style={{ width: '70%' }} /></div><p>6 hours left this week <ArrowRight /></p></div><div className="stat-card"><div className="stat-icon purple"><GraduationCap /></div><span>Topics mastered</span><strong>18</strong><p className="muted">+4 this month <span className="positive">↗ 28%</span></p></div><div className="stat-card"><div className="stat-icon orange"><Clock3 /></div><span>Study time</span><strong>6h 42m</strong><p className="muted">This week <span className="positive">↗ 12%</span></p></div></div><div className="activity-header"><h3>Recent activity</h3><button onClick={() => setActive('My progress')}>View all <ArrowRight /></button></div><div className="activity-list">{activities.map(item => <div className="activity-row" key={item.title}><div className={`activity-icon ${item.color}`}>{item.color === 'violet' ? <FileText /> : <Play />}</div><div className="activity-name"><strong>{item.title}</strong><span>{item.subject}</span></div><div className="activity-score"><strong>{item.score}</strong><span>Quiz score</span></div><span className="activity-time">{item.time}</span><MoreHorizontal /></div>)}</div></section>}</div></main></div> }
